import { SurnameShortcutPipe } from './surname-shortcut.pipe';

describe('SurnameShortcutPipe', () => {
  it('create an instance', () => {
    const pipe = new SurnameShortcutPipe();
    expect(pipe).toBeTruthy();
  });
});
