import { ImportantDirective } from './important.directive';

describe('ImportantDirective', () => {
  it('should create an instance', () => {
    const directive = new ImportantDirective();
    expect(directive).toBeTruthy();
  });
});
